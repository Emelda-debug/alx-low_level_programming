#include "main.h"
/**
*mul- code to multipy 2 numbers
*@a: integer to be multiplied
*@b: integer to be multiplied
*return: 0 means success
*/

int mul(int a, int b)
{
int mul;
mul = a*b;
return (mul);
}

