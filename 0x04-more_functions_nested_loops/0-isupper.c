#include "main.h"

/**
*_isupper- Code to check for an uppercase letter
*@c: Caiable text to check text case
*return: 0 means success
*/

int _isupper(int c)

{
if (c >= 'A' && c <= 'Z'){
return (1);
}
else{
return (0);
}
}
